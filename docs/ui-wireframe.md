# TransitLog P0 界面线框（文字版）

> 视觉基调：延续 MemoRise 的**浅色纸感**（暖灰底 `#F3F4F6` + 白卡片 + 圆角），主色沿用同族绿色系；
> 深色主题另配一套 token（见 [requirements-review.md](./requirements-review.md#3-主题token体系)）。
> 交互原则：**卡片式列表** + **浮动加号(FAB)**，与 MemoRise 的 Material 风格一致。

---

## P0 页面清单

1. 本地登录/注册页
2. 主界面（行程列表）
3. 新增/编辑行程对话框
4. 统计页
5. 设置页（主题切换、本地账户、数据管理）

> 2026-08-03 确认：**无云同步、无云数据库**。账号为本地登录，数据仅存本机。

---

## 1. 本地登录 / 注册页（启动后未登录才显示）

```
┌──────────────────────────────────────────────┐
│  TransitLog                                   │  ← 标题区（可放 logo）
│                                               │
│    ┌──────────────────────────┐               │
│    │  用户名  [   cd@2333      ]              │
│    │  密码    [  ••••••        ]              │
│    │   [ 登录 ]   [注册]  (Tab 切换)          │
│    │  说明: 账号仅保存在本机，不会上传         │
│    └──────────────────────────┘               │
│                                               │
└──────────────────────────────────────────────┘
```

- 登录/注册双 Tab，注册含"确认密码"；密码加盐哈希存 SQLite。
- 底部一行**说明**：数据仅保存在本机（无云同步）。

## 2. 主界面（行程列表）—— 核心 P0

```
┌──────────────────────────────────────────────┐
│ ☰ TransitLog        cd@2333    [浅色]  ◉       │ ← 顶栏: 侧栏/当前账号/主题切换/用户菜单
├──────────────────────────────────────────────┤
│  ▣ 8月3日 周六         今日 3 段 · 12.4 km      │ ← 按日期分组的表头
│  ┌──────────────────────────────────────────┐ │
│  │ 🚇 地铁 [通勤] 海淀黄庄 → 北京大学东门       │ │ ← 图标+中文名 [标签] | 起→终
│  │  08:12–08:31 · 3.2 km · ¥4.00             │ │        时间区间 · 里程 · 费用
│  │  (备注: 早高峰人少)                        │ │        备注(有才显示)
│  └──────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────┐ │
│  │ 🚗 驾车 [进行中] 未名湖 → (行驶中)          │ │ ← end_time 为空 → 进行中徽标
│  │  09:20–现在 · -- km · ¥--                  │ │        里程/费用未填则显示 --
│  └──────────────────────────────────────────┘ │
│  ▣ 8月2日 周五          昨日 5 段 · 18.7 km      │
│  ┌──────────────────────────────────────────┐ │
│  │ ...                                        │ │
│  └──────────────────────────────────────────┘ │
│                                               │
│                            ┌─────┐            │
│                            │  ＋  │  ← FAB 记行程 │
│                            └─────┘            │
└──────────────────────────────────────────────┘
```

- **卡片 = `TripRecord` 数据行**，左侧交通方式图标（来自 `transport_mode.icon`），`[标签]` 来自 `trip_tag`。
- **进行中行程**（`end_time` 为空）显示徽标，里程/费用未填显示 `--`，不显示 NaN。
- 顶栏右侧：当前账号、主题切换按钮、用户菜单（下拉: 设置/退出登录）。
- 搜索/筛选（P1）：顶部搜索框暂不做。

## 3. 新增 / 编辑行程对话框

```
┌───────────────────────────────┐
│ 记一段行程              [取消][保存] │
│  ┌──────────────────────────┐ │
│  │ 交通方式  (🚇地铁 🚌公交  🚶步行 │ │ ← 图标宫格，数据来自 transport_mode
│  │            🚲骑行 🚗驾车  ✈️飞机) │ │    空白态: 至少显示"其他"
│  └──────────────────────────┘ │
│  起点  [  海淀黄庄          ]  📍   │ ← 📍 可选: 拾取坐标(填 lat/lng)
│  终点  [  北京大学东门      ]  📍   │
│  开始  [ 2026-08-03 08:12 ]       │ ← QDateTimeEdit，默认"现在"
│  结束  [ 2026-08-03 08:31 ] (□进行中)│
│  里程  [ 3.2 ] km  ↺自动计算       │ ← 有坐标才可自动算，否则手填
│  费用  [ 4.00 ] 元                │
│  标签  [ 通勤 ▾ ] (可留空)        │ ← 来自 trip_tag 字典，可留空
│  备注  [ 早高峰人少            ]   │
└───────────────────────────────┘
```

- 保存前**校验**：起点/终点至少填一个、时间合法（结束>开始，除非"进行中"）。
- 空数据容错：全部字段可留空只填时间也允许保存，但不能整行全空。
- "进行中"勾选 → `end_time` 存 NULL；标签留空 → `tag_id` 存 NULL。

## 4. 统计页（P0 简化版）

```
┌──────────────────────────────────────────────┐
│ ☰ 统计                                        │
│  [本月] [近7天] [近30天]   ← 时间范围 SegBtn     │
│  ┌────────┬────────┬────────┐                 │
│  │ 总里程  │ 总费用  │ 行程数  │  ← 三栏指标卡     │
│  │ 186.5km│ ¥128.40│ 42 段   │                 │
│  └────────┴────────┴────────┘                 │
│  ┌──────────────────────────────┐             │
│  │ 每日里程折线图 (近7天)          │  ← QChart     │
│  └──────────────────────────────┘             │
│  ┌──────────────────────────────┐             │
│  │ 交通方式占比: 🚇 48% 🚌 32% 🚶 20%  │  ← 简易横向条  │
│  └──────────────────────────────┘             │
└──────────────────────────────────────────────┘
```

- 空数据态：显示"暂无行程，去记一笔吧" + 按钮跳新增，**不崩溃、不显示 NaN**。

## 5. 设置页

```
┌──────────────────────────────┐
│ ☰ 设置                         │
│ ┌─ 外观 ───────────────────┐ │
│ │  主题        [浅色/深色/跟随系统] │ │  ← 切换立即生效(ThemeManager)
│ └──────────────────────────┘ │
│ ┌─ 账户 ───────────────────┐ │
│ │  当前账号  cd@2333        │ │
│ │  [修改密码] [退出登录]      │ │
│ └──────────────────────────┘ │
│ ┌─ 数据管理 ───────────────┐ │
│ │  数据库位置  C:/Users/.../TransitLog.db │ │ ← 只读显示
│ │  [导出备份 .db]            │ │
│ │  [清空本地数据](需二次确认)  │ │
│ └──────────────────────────┘ │
└──────────────────────────────┘
```

> 无云同步设置项；数据管理提供备份导出与清空（二次确认）。

---

## 与 MemoRise 的样式对照

| 元素 | MemoRise 已有 | TransitLog 复用 |
|---|---|---|
| 背景/卡片 | `#f3f4f6` 底 + `#fff` 卡片 + 圆角 14px | 直接沿用 |
| 主色 | 绿 `#4A7C6F` / hover `#3D6B5E` | 沿用同族 |
| 按钮 | 主按钮实心绿 / 次按钮白底绿边 | 沿用 |
| 主题 | 只有浅色 + 背景图 | **新建深色 token 集**（见评审文档） |

> ⚠️ 注意：MemoRise 当前**没有深色主题**，也没有真正 token 抽象（颜色是硬编码十六进制）。所以"复用 token 体系"实际要做的是**先抽 ThemeManager**，详见需求评审文档第 3 点。
